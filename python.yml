---
  - name: "Playing with Ansible and Git"
    hosts: localhost
    connection: local 
    tasks:

    - name: "do something with meraki"
      shell: "./bump.py --net '{{ meraki_net }}' --serial '{{ meraki_serial }}'"
      register: "output"

    - debug: var=output.stdout_lines
